import { Helmet } from 'react-helmet-async';
const Menu = () => {
    return (
        <div>
            <Helmet>
                <title>Bistro Boss | menu</title>
            </Helmet>
            <p>out menu</p>
        </div>
    );
};

export default Menu;